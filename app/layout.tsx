import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "TypeNinja - Master Your Typing Skills",
  description:
    "Welcome to TypeNinja! Sharpen your typing speed and accuracy with our engaging typing tests and exercises. Join now to become a typing master in a fun and interactive way.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
